<h2>User info</h2>
<%= attachment_image_tag(user, :profile_image,size:'100x100') %>

<table class="table">
	<tr>
		<td>name </td>
		<td><%=  current_user.name %></td>
	</tr>
	<tr>
		<td>introduction</td>
		<td><%=  current_user.introduction%></td>
	</tr>
</table>
<div class="row">
	<!-- type="button"でボタン生成　class="btn-block" 横幅いっぱいボタン -->
	<%= link_to "", edit_user_path(current_user.id), class:"col-xs-12 btn btn-default glyphicon glyphicon-cog "%>
</div>

<h2>New book</h2>
<div class="row">
	<%= form_for(book) do |book|%>
		<p>Title</p>
		<%= book.text_field :title, :size=>"40" %>
		<p>Opinion</p>
		<%= book.text_field :body, :size=>"40"  %>
		<p></p>
		<div class="row">

			<!-- <%=%>内にclassを利用する場合　class:で<>内で利用するときはclass=を使う -->
				<%= book.submit "Create Book", class: "col-xs-12 btn-primary" %>
			<!-- <button name="button" type="submit", class="col-xs-12" >Create Book</button> -->
	</div>
	<% end %>
</div>

book/show.html.erb

<%= render "layout/side" %>

1. "_○○.html.erb"(部分テンプレートファイル)のインスタンス変数の@を外す。
2. <%= render "部分テンプレートのファイル名", book: @book %>

以上

~~~