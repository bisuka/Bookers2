<body>
	<div class="container box">
		<div class="row">
		    <div class="col-xs-3">
		    	<span id="notice"><%=  notice %></span>
				<div class="box1">
					<section class="Userinfo">
						<h2>User info</h2>
						<%= attachment_image_tag @user, :profile_image,size:'100x100',fallback: "no_image.jpg" %>
						<table class="table">
							<tr>
								<td>name </td>
								<td><%=  @user.name %></td>
							</tr>
							<tr>
								<td>introduction</td>
								<td><%= @user.introduction%></td>
							</tr>
						</table>
						<div class="row">
							<!-- type="button"でボタン生成　class="btn-block" 横幅いっぱいボタン -->
							<%= link_to "", edit_user_path(@user.id), class:"col-xs-12 btn btn-default glyphicon glyphicon-cog "%>
						</div>

						<h2>New book</h2>
						<div class="row">
							<%= form_for(@book) do |book|%>
								<p>Title</p>
								<%= book.text_field :title, class: "col-xs-12" %>
								<p>Opinion</p>
								<%= book.text_area :body, class: "col-xs-12"  %>
									<!-- <%=%>内にclassを利用する場合　class:で<>内で利用するときはclass=を使う -->
									<%= book.submit "Create Book", class: "col-xs-12 btn-primary" %>
									<!-- <button name="button" type="submit", class="col-xs-12" >Create Book</button> -->
							<% end %>
						</div>
				</div>
			</div>
			<div class="col-xs-9">
				<table class="box2 table">
					<caption></caption>
					<thead>
						<tr>
							<h3>Book detail</h3>
						</tr>
					</thead>
					<tbody>
						<tr>
						</tr>
					</tbody>
					<tbody>

						<tr>
							<td>
								<%=link_to attachment_image_tag(@user, :profile_image,size:'50x50',fallback: "no_image.jpg"), user_path(@book_id.user.id) %><br>
								<%= link_to @book_id.user.name, user_path(@book_id.user.id) %>
							</td>
							<td><%= @book_id.title %></td>
							<td><%= @book_id.body %></td>
							<% if @user == current_user %>
								<td><%= link_to "Edit" , edit_book_path(@book_id) %></td>
								<td><%= link_to "Destroy", book_path(@book_id), method: :delete  %></td>
							<% end %>
						</tr>


						<!-- 本の詳細ページへ行く時全てのデータが表示されてしまっていた失敗例 -->
						<!-- 必要な情報は一つの<Book id: x, title: "x", body: "x", user_id: x>とuser infoに表示されているその本の投稿者のuserテーブル
							 <User id: nil, email: "", introduction: nil, profile_image_id: nil, created_at: nil, updated_at: nil, name: nil> -->

		<!-- 			<% @books.each do |book| %>
						<tr>
							<td><%=link_to attachment_image_tag(@user, :profile_image,size:'50x50'), user_path(current_user.id) %></td>
							<td><%= link_to book.title, book_path(book.id) %></td>
							<% if @user == current_user %>
								<td><%= link_to "Edit" , edit_book_path(book.id) %></td>
								<td><%= link_to "Destroy", book_path(book.id), method: :delete  %></td>
							<% end %>
						</tr>
						<tr><td><%= link_to @user.name, books_path %></td></tr>
						<% end %> -->
					</tbody>
				</table>
			</div>
		</div>
	</div>
</body>